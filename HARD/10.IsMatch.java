package top.nzhz;


public class IsMatch {
    public boolean isMatch(String s, String p) {
        int slen = s.length(), plen = p.length();

        int i=0;
        if(plen==0&&slen!=0)
            return flase;
        if(slen==0){
            
        }
        while(i<slen){

        }
    }
}